[package]
name = "damaai-pet"
version = "0.1.0"
description = "Desktop-Resident AI Growth Pet"
authors = ["Kodari Dev Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.dev]
incremental = false
codegen-units = 1
